# Terminal GPT Configuration
# Generated by setup wizard - your API token is stored securely in system keychain

# NOTE: It's recommended to run the setup wizard instead of manually editing this file:
#   ./ask --setup

# Required: OpenRouter model to use (format: provider/model)
LLM=anthropic/claude-haiku-4.5

# Optional: Enable/disable markdown rendering (default: true)
RENDER_MARKDOWN=true

# Optional: Enable/disable surrounding panels/boxes (default: true)
SHOW_PANELS=true

# Optional: Show token usage and cost after each response (default: false)
SHOW_COST=false

# API token storage:
# - Preferred: Stored in system keychain (use ./ask --setup)
# - Fallback: Set environment variable ASK_API_TOKEN
# - Legacy: Add API_TOKEN=your_token_here (not recommended for security)
